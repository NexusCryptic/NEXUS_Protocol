# Gemma Resonance Convergence

## Scope

This record connects the newly reported Gemma Resonance Gambit to the Living System without conflating it with upstream Gemma code or with other similarly named projects.

## Confirmed related project: `machackabook/GMNINEXUS`

The repository README identifies **Gemma-GLM v1.0 — Geometric Language Machine** as a deterministic LLM companion layer. It describes a zero-trainable-parameter hybrid using a CRG semantic graph, exact rational math, Python sandboxing, visual NRCI, resonance/veto scoring, Hugging Face `LogitsProcessor` integration, speculative draft/verify, KV pruning, ValueGeometry, and an interactive agent.

The repository's current tree includes `gemma_glm.py`, `ubp_unified_v5.py`, `value_geometry.py`, `llm_glm/`, `gemma-glm-v1.0-paper.pdf`, and `MOBILE_INSTALL.md`.

The repository's latest visible commit is `470c99214908686ecd0d0f433dc5936fff90c928`, dated 2026-07-21, adding `gemma-glm-v1.0-paper.pdf`. That commit was authored by `DigitalEuan` and committed by `web-flow`.

**Therefore:** `GMNINEXUS` is **CORRELATED**, not automatically canonical and not automatically attributable to the Architect.

## Canonical integration target

```text
Gemma / GLM research
        │
        ▼
Resonance Adapter Contract
        │
        ├── proposal scoring
        ├── constraint/veto signals
        ├── provenance attachment
        ├── uncertainty state
        └── verification hooks
        │
        ▼
NEXUS_CORE → NEXUS_Protocol → The-Ledger
```

The protocol layer owns the contract. Core owns implementation. The Ledger owns provenance. External/upstream code remains external unless a separate derivation record proves what was incorporated.

## Proposed sector mapping

| Gemma/GLM capability | Living System sector | Promotion condition |
|---|---|---|
| CRG semantic graph | Memory / Cartographer | graph schema and lineage verified |
| Resonance scoring | AI Gateway / Reflection | deterministic input/output contract |
| Hard veto | Governance / Safety | policy and failure cases tested |
| Exact math | Verification Engine | reproducible test vectors |
| Vision NRCI | Multimodal Runtime | artifact provenance retained |
| HF LogitsProcessor | Provider Adapter | provider boundary documented |
| Speculative draft/verify | AI Council | model/provider separation verified |
| KV pruning | Runtime optimization | benchmark + correctness evidence |
| ValueGeometry | Geometry / Cartographer | mathematical definition and tests |
| Mobile install | Portable Runtime | platform-specific manifest |

## The reported Gemma Resonance Gambit

The Architect has reported a newly added artifact under this name and a recent GitHub push. The exact repository object has not yet been positively matched by the current GitHub search index.

```yaml
status: INCOMING
name: Gemma Resonance Gambit
canonical_target: NEXUS_CORE
protocol_target: NEXUS_Protocol
ledger_target: The-Ledger
verification:
  git_commit: pending
  git_blob: pending
  sha256: pending
  iso: pending
  beat: pending
```

No external Gemma repository is substituted for this artifact. The exact object must be identified on the next pass.

## Non-destructive rule

No source repository is rewritten to make it fit the canonical architecture. Integration occurs through adapters, manifests, provenance records, tests, and explicit promotion commits.
